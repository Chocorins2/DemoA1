<!-- Author: Ngoc Phuong Uyen Ho -->
<!-- Student ID: 301103427 -->
<!-- Date: Sep 30th 2021 -->

<h1><%= title %></h1>

<div class="row">
    <div class="col">

        <!-- <% if(displayName) { %>
        <a href="/clothing-list/add" id="addButton" class="btn btn-primary mb-1"><i class="fas fa-plus-circle"></i> Add Clothing Item</a>
        <% } %> -->

        <table class="table table-striped table-bordered table-hover">
            <thead class="table-dark">
              <tr>
                    <th class="text-center col-1">Contact Name</th>
                    <th class="text-center col-1">Contact Number</th>
                    <th class="text-center col-4">Contact Email</th>
                 <% if(displayName) { %>
                  <th></th>
                  <th></th>
                <% } %>
              </tr>
            </thead>
            <tbody>

             <!-- Repeatable Row Template -->   

             <% for(let count = 0; count < Contact.length; count++) { %>
              
                <tr>
                    <td class="text-center col-1"><%= Contact[count].name %></td>
                    <td class="text-center col-2"><%= Contact[count].number %></td>
                    <td class="col-4"><%= Contact[count].email %></td>
                     <% if(displayName) { %>
                      <td class="text-center"><a href="/Contact-list/edit/<%= Contact[count]._id %>" class="btn btn-primary btn-sm edit"><i class="fas fa-edit fa-sm"></i> Edit</a></td>
                      <td class="text-center"><a href="/Contact-list/delete/<%= Contact[count]._id %>" class="btn btn-danger btn-sm delete"><i class="fas fa-trash-alt fa-sm"></i> Delete</a></td>
                    <% } %>
                </tr>

              <% } %>

            </tbody>
          </table>
    </div>
</div>